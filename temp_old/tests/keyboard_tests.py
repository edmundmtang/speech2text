import keyboard
import time
import threading

def on_press(key):
    if key == keyboard.Key
    print(



with keyboard.Listener(on_press=on_press, on_release=on_release) as listener:
    listener.join()

